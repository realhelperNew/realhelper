# Example environment variables for local development
PORT=3000
# Optional: SendGrid settings (leave blank or set in your CI/host)
SENDGRID_API_KEY=
SENDGRID_FROM=you@example.com
# Add any other secrets as env vars, do NOT commit real values
# DATABASE_URL=sqlite://:memory:
